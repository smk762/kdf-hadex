{
  "name": "kdf-hadex-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "npm run gen-styles && vite build",
    "preview": "vite preview --port 5173",
    "typecheck": "tsc --noEmit",
    "ci": "npm run typecheck && npm run build",
    "tidy-www": "bash ./scripts/tidy-www.sh",
    "gen-styles": "node ./scripts/gen-styles.js"
  },
  "devDependencies": {
    "vite": "^5.3.0",
    "typescript": "^5.4.0",
    "tslib": "^2.6.0",
    "@types/node": "^20.9.1",
    "lit": "^2.7.5",
    "rollup-plugin-copy": "^3.4.0"
  }
}
